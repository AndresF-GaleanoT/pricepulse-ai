[Unit]
Description=Cloudflare Quick Tunnel para PricePulse API
After=network.target

[Service]
Type=simple
ExecStart=cloudflared tunnel --url http://localhost:8000
Restart=on-failure
RestartSec=5
User=root

[Install]
WantedBy=multi-user.target
